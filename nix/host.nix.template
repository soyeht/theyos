# theyOS service configuration.
# Generated by ./install-nixos — edit this file to change theyOS settings,
# then re-run ./install-nixos to apply.
#
# For machine-specific settings (boot loader, networking, SSH, users, etc.),
# edit nix/base-configuration.nix instead.
{ ... }:

{
  services.theyos = {
    enable = true;
    user = "«DETECTED_USER»";
    repoDir = "/home/«DETECTED_USER»/theyos";
    domain = "«PROMPTED_DOMAIN»";
    accessMode = "«PROMPTED_ACCESS_MODE»";

    # Uncomment and configure to enable optional integrations:
    # cloudflare.enable = true;
    # cloudflare.tokenFile = "/home/«DETECTED_USER»/.cloudflared/soyeht.token";
    # cloudflare.configFile = "/home/«DETECTED_USER»/theyos/distro/cloudflared/config.yml";
    # tailscale.enable = true;
    # dnsmasq.enable = true;
    # dnsmasq.interface = "wlp58s0";
  };
}

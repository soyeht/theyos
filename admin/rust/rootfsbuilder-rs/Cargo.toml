[package]
name = "rootfsbuilder-rs"
version = "0.1.27"
edition.workspace = true
rust-version.workspace = true
description = "Build Ubuntu 24.04 ext4 rootfs for Firecracker VMs (replaces build-rootfs.sh)"

[[bin]]
name = "rootfsbuilder"
path = "src/main.rs"

[dependencies]
core-rs = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true

[package]
name = "launcher-rs"
version = "0.1.27"
edition.workspace = true
rust-version.workspace = true
description = "Admin backend launcher — loads .env, resolves binaries, validates preflight, execs server (replaces run-backend-host.sh)"

[[bin]]
name = "theyos-admin-host"
path = "src/main.rs"

[dependencies]
core-rs = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
